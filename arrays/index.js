const chunk = require('./chunk');
const compact = require('./compact');
const drop = require('./drop');
const dropWhile = require('./dropWhile');
const take = require('./take');
const zip = require('./zip');

module.exports = {
  chunk, compact, drop, dropWhile, take, zip,
};
